services:
  postgres:
    image: pgvector/pgvector:pg17
    container_name: airagservice-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: airagservice
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d airagservice"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
